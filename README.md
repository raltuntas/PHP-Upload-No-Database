# PHP-Upload-No-Database
simple php web upload with no database. all uploaded files stored in txt file. uploader will be able to to temporarily view all his/her uplaoded files as long as session is valid.
